namespace AWSConsole.Uri.Builder.XRay.Metadata
{
    internal struct Constants
    {
        public const uint MaxMinutes = 360;
        public const string DateTimeFormatString = "yyyy-MM-dd'T'HH:mm:ss.fffZ";
        public const string timeRange = nameof(timeRange);
        public const string filter = nameof(filter);

    }
}
